// use super::list::ListMatcher;
use super::matcher::Matcher;
use crate::common::parser::Span;
use crate::error::Result;

pub fn parse_matcher(input: &str) -> Result<Box<dyn Matcher>> {
    unimplemented!();
}
